Source: [src/control_plane_kit_server_sdk/_control_dispatch.py](../../../../src/control_plane_kit_server_sdk/_control_dispatch.py).
Maintain this document alongside its source file. When the source or relevant imported contracts change, verify and update this companion in the same change.

This private verification-extra owner captures one installation's receiving context. Pure configuration validation runs before host collision checks and again at preparation before any descriptor call. Preparation snapshots each descriptor once, builds one result codec per registered variable, records sorted installed names and allocates replay only for legacy/mixed configurations. Frozen fields hide all context from repr; the contained registry is trusted conventional state, not deep immutability or a sandbox. Two preparations have separate registry/replay objects.

The moved static interpreter admits the dedicated grant, binds installed target/declaration and emits the same Core capabilities/status bytes. Static status is registry coverage. The moved variable interpreter admits the actual operation/variable/candidate, binds target before registry lookup, preserves READ/APPLY result compaction and delegates unchanged replay behavior to its existing owner. No new verifier, callback protocol, HTTP parser, threadpool, framework import or durable state exists here. Health is the existing neutral dispatcher captured by identity, not reimplemented.

HTTP adapters own extraction/scheduling/publication; they consume these fixed operations and one prepared value. Private registry errors retain existing diagnostics during this extraction; public FastAPI errors remain fixed. #27 consumes the seam only after #26 acceptance. See decision0017 and the full inherited SDK tests plus the preparation/installed-import laws.
