Source: [src/control_plane_kit_server_sdk/py.typed](../../../../src/control_plane_kit_server_sdk/py.typed).
Maintain this document alongside its source file. When the source or relevant imported contracts change, verify and update this companion in the same change.

This empty PEP 561 marker is explicitly included through
[pyproject.toml](../../pyproject.toml.md), making inline annotations discoverable
to type-checking consumers. It contains no runtime behavior. Its presence does
not mean a type checker was run or that runtime protocol membership checks
signatures. Keep it coordinated with package-data and installed-artifact checks.
